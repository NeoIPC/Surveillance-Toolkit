Gizem Karadağ; Adrie Bekker; Angela Dramowski; Paul Heath; Begoña Loureiro_Gonzalez; Jose_Ignacio Pijoan_Zubizarreta; Brar Piening
:doctype: book
// PDF/HTML document metadata. asciidoctor-pdf has no native dc:rights field, so the licence is recorded
// in Keywords (and stated in full on the "Licensing and attribution" colophon page).
:keywords: NeoIPC, neonatal infection surveillance, VLBW, VPT, healthcare-associated infection; licence: protocol text and List of Infectious Agents CC BY 4.0, List of Antibiotics CC BY-NC-SA 3.0 IGO
:imagesdir: ./img
// Where THIS culture's localized fragments live, relative to doc/protocol -- the directory po4a writes a
// translated source into. The invariant source is the directory root, so `.` is both the default and what
// an English build uses; a localized build is rendered from doc/protocol/<culture>/ with asciidoctor's
// --base-dir still pointing at doc/protocol, and the build passes the culture name here.
//
// It exists because the two reference classes in this document resolve to different places and cannot
// share one root: definitions and the generated lists are localized and live beside the translated
// protocol, while the header, the images and the PDF theme are shared and live at the root. Without it a
// localized build reads either the English definitions or no images, depending which root you pick.
//
// The build always passes this, so the ifndef only fires when the document is rendered by hand.
ifndef::locale-dir[:locale-dir: .]
ifdef::lang[:title-page-background-image: image:NeoIPC-Core-Title-Page.{lang}.svg[]]
ifndef::lang[:title-page-background-image: image:NeoIPC-Core-Title-Page.svg[]]
:revsuffix: 
ifdef::revremark[]
:revsuffix: -{revremark}
ifdef::lang[:page-foreground-image: image:Preview-Watermark.{lang}.svg[]]
ifndef::lang[:page-foreground-image: image:Preview-Watermark.svg[]]
endif::revremark[]
:title-logo-image: image:LOGO_NEOIPC.png[pdfwidth=60mm,align=center]
include::../locale/attributes.adoc[]
ifdef::backend-html5[]
:full-width: role=full-width
endif::[]
ifdef::backend-pdf[]
:full-width: pdfwidth=100%
:pdf-theme: {docdir}/../NeoIPC.theme.yml
// Where the theme looks for the faces it names. This repository's own fonts come first so that the
// figures are DRAWN from the same files the generator MEASURED; the gem's directory stays on the list
// because the theme still names asciidoctor-pdf's own bundled faces for everything else.
//
// {docdir} is doc/protocol for every culture -- the build pins --base-dir there so that shared
// references keep working when the source being rendered is one level down -- so this one path serves
// the localized builds unchanged.
:pdf-fontsdir: {docdir}/../../common/fonts;GEM_FONTS_DIR
endif::[]
ifdef::backend-docbook5[]
:full-width: scaledwidth=100%
endif::[]
:table-stripes: odd
:sectnums:
:hide-uri-scheme:
:stem:
:toc: